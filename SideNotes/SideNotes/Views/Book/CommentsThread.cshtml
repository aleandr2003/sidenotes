@model Paragraph
@{
    ViewBag.Title = "Комментарии";
}
@section HEAD{
    <script src="@Url.Content("~/Scripts/BookHeader.js")" type="text/javascript"></script>
}
<div id="main" class="commentThread">
@Html.Partial("Header", Model.Book)
<div class="bookBlock">
    @Abzats.Render(Model)
</div>
@Html.ActionLink("Читать книгу с этого места", "View", "Book",
            new { id = Model.Book_Id, skip = Model.OrderNumber - 1 }, new { @class = "toTextButtonLink" })

@Html.Action("HeadIndex", "Comment", new { headCommentId = ViewBag.HeadCommentId })
</div>